
spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=password
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

spring.jpa.hibernate.ddl-auto = create
#for enabling file uploading
spring.servlet.multipart.enabled=true
# Threshold after which files are written to disk.
spring.servlet.multipart.file-size-threshold=2KB
# Max file size.
spring.servlet.multipart.max-file-size=20MB
# Max Request Size
spring.servlet.multipart.max-request-size=21MB
#if working on windows machine , replace '/' with '\'
#file.upload-dir=src/main/resources/static/images
file.upload-dir=media
#mail properties
spring.mail.host=smtp.gmail.com
spring.mail.port=587
#write here your gmail address
spring.mail.username=berkaltug96@gmail.com
#this must be gmail's generated app password , not gmail account password itself.
#go to https://myaccount.google.com/apppasswords and create one for your gmail.
spring.mail.password=******
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000